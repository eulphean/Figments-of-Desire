#pragma once
#include "ofMain.h"
#include "ofxBox2d.h"

// Subsection body that is torn apart from the actual texture and falls on the ground. 
class World {
  public:
    void setup();
    void update();
    void draw();
    
  private:
  
};
